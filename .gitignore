node_modules/

# Datos reales del Gantt (se generan en la PC servidor a partir de los .seed.csv).
# Se ignoran para que un futuro `git pull` nunca sobreescriba datos ya cargados.
backend/data/projects.csv
backend/data/tasks.csv
backend/data/users.csv
backend/data/independent_tasks.csv
backend/data/audit_log.csv
backend/data/*.tmp-*

backend/data/access_log.jsonl

# Private delivery artifacts and backups supplied by the user.
entrega-jefes/

# Credenciales y respaldos locales de la migracion; nunca se publican con el codigo.
claves-usuarios.json
backend/data-backup-*/
